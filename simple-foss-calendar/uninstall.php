<?php
/**
 * Uninstall handler for Simple FOSS Calendar.
 *
 * The plugin intentionally leaves events in place by default. Site owners can
 * delete the Events post type content manually before removing the plugin.
 *
 * @package SimpleFossCalendar
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
